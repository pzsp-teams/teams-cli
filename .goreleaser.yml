version: 2

before:
  hooks:
    - go mod tidy
    - go mod download

builds:
  - goos:
      - linux
    goarch:
      - amd64
    ldflags:
      - -s -w
    hooks:
      post:
        - upx "{{ .Path }}"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs(!)?(\(.+\))?:'
      - '^test(!)?(\(.+\))?:'
      - '^chore(!)?(\(.+\))?:'
      - 'merge conflict'
      - 'Merge pull request'
      - 'Merge remote-tracking branch'
      - 'Merge branch'

release:
  github:
    owner: "{{ .Env.GITHUB_REPOSITORY_OWNER }}"
    name: "{{ .ProjectName }}"
  name_template: "{{.Tag}}"
  draft: false
  prerelease: auto
