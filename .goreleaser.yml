version: 2

before:
  hooks:
    - go mod tidy
    - go mod download

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
    hooks:
      post:
        - upx "{{ .Path }}"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs(!)?(\(.+\))?:'
      - '^test(!)?(\(.+\))?:'
      - '^chore(!)?(\(.+\))?:'
      - 'merge conflict'
      - 'Merge pull request'
      - 'Merge remote-tracking branch'
      - 'Merge branch'

release:
  github:
    owner: "{{ .Env.GITHUB_REPOSITORY_OWNER }}"
    name: "{{ .ProjectName }}"
  name_template: "{{.Tag}}"
  draft: false
  prerelease: auto
